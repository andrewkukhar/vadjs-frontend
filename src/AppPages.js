// import React from 'react';
// import Navbar from 'components/Navbar';
// import Sidebar from 'components/Sidebar';

// const renderRoute = (route) => (
//   <Route
//     key={route.id}
//     path={route.path ? route.path : route.id}
//     element={
//       <ProtectedRoute user={user} role={route.role}>
//         {createElement(componentMap[route.id])}
//       </ProtectedRoute>
//     }
//   />
// );

//   <AppTemplate>
//     <div className="app-body">
//       <Routes>
//         {links.map(renderRoute)}
//         {hiddenRoutes.map(renderRoute)}
//       </Routes>
//     </div>
//   </AppTemplate>


// const AppLayout = ({ children }) => {
//   return (
//     <>
//       <Navbar />
//       <Sidebar />
//       {children}
//     </>
//   );
// };

// export default AppLayout;
